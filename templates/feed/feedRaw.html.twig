<div class="media text-muted pt-3">
    {% set initials = stamp.user.fullName|split(' ') %}


    <div class="stamp-raw   ">
        <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <span class="d-block">
        <p> User
            <a href="{{ path('stamp_user', {'id': stamp.user.id}) }}">
                <strong class="text-gray-dark">@{{ stamp.user.username }}</strong>
            </a>
            has added a new stamp
            <small>· {{ stamp.time|date("d/m/y h:i") }}</small></span>
        </p>
        <div class="row mb-1">
            <div class="col-sm-auto">
                <a href="{{ asset('uploads/' ~stamp.image) }}" target="_blank"><img width="160" src="{{ asset('uploads/' ~stamp.image) }}"></a>
            </div>
            <div class="col-sm">
                <strong>Description:</strong> {{ stamp.description }}<br>
                <strong>Color: </strong>{{ stamp.color }}<br>
                <strong>Serial Number: </strong> {{ stamp.serialnumber }}<br>
                <strong>Country: </strong> {{ stamp.country.country }}<br>
                <strong>Year: </strong> {{ stamp.year }}<br>
                {% if stamp.category is not null %}
                <strong>Category: </strong> {{ stamp.category.category }}
                {% else %}
                <strong>Category: </strong> none <br><br>
                {% endif %}
            </div>
        </div>
        {% if is_granted('ROLE_USER') %}
            <a class="btn btn-outline-primary mb-2" href="{{ path('stamp_show', {'id': stamp.id}) }}">Add comment</a>
        {% endif %}
    </div>
</div>