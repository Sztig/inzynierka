<div class="media text-muted pt-3">
    {% set initials = stamp.user.fullName|split(' ') %}
    <div class="stamp-raw   ">
    <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <span class="d-block">
    <p> User
            <a href="{{ path('stamp_user', {'id': stamp.user.id}) }}">
                <strong class="text-gray-dark">@{{ stamp.user.username }}</strong>
            </a>
        has added a new stamp
            <small>· {{ stamp.time|date("d/m/y h:m") }}</small></span>
    </p>
        <div class="row">
        <div class="col-sm-auto">
    <a href="{{ asset('uploads/' ~stamp.image) }}" target="_blank"><img width="160" src="{{ asset('uploads/' ~stamp.image) }}"></a>
        </div>
        <div class="col-sm">
        Name: <a href="#">{{ stamp.name }}</a><br>
        Description: {{ stamp.description }}<br>
        Color: {{ stamp.color }}<br>
        Serial Number: {{ stamp.serialnumber }}<br>
        Country: {{ stamp.country.country }}
        </div>
        </div>
    </div>
</div>